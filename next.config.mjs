/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js App Router doesn't support the built-in i18n config anymore
  // We've implemented our own i18n solution with URL prefixes and i18next
};

export default nextConfig;
